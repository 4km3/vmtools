#!/usr/bin/env bash

: "${1?"Usage: $0 VM_name"}"

for vm in "$@"; do
  virsh -c qemu:///system destroy "$vm"
  virsh -c qemu:///system undefine "$vm"
  sudo rm -r /var/lib/libvirt/images/"$vm".qcow2
done
